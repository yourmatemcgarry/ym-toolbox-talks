// This file is no longer used.
//
// Toolbox talks now live in data/talks.json (plain JSON, not a JS module) so
// the "Add a toolbox talk" page on the site can update it automatically via
// a Netlify Function. You can safely delete this file — it's kept only so
// nothing breaks if something still points at it.
